{
  // Raiz "solucion": solo enlaza los proyectos. Las opciones van en cada uno
  // (aqui serian ignoradas por `tsc --build` y solo confunden).
  "files": [],
  "references": [
    {
      "path": "./tsconfig.node.json"
    },
    {
      "path": "./tsconfig.app.json"
    }
  ]
}
